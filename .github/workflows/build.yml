name: Build

on: 
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10"]
    steps:
    - uses: actions/checkout@v4
    - name: Test
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        docker build --build-arg GITHUB_TOKEN="$GITHUB_TOKEN" -t pytest-chrome -f tests/Dockerfile .
        docker run --shm-size=2g pytest-chrome
    
